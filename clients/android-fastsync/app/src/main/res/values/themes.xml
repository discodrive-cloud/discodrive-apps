<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The light half; values-night carries the dark parent. The platform has no
         DayNight.NoActionBar to inherit from, so the split is done by qualifier.
         Compose paints everything above this (see FastSyncTheme) — what matters here is
         the window background, which must not flash the wrong shade at launch. -->
    <style name="Theme.FastSync" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
